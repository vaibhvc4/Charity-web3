RUN
```shell
git clone https://github.com/vaibhvc4/Charity-web3.git
cd Charity-web3
npm install
npm install --save-dev hardhat
npx hardhat compile
npx hardhat run scripts/deploy.js --network sepolia
pip install -r requirements.txt
uvicorn main:app --reload
```
